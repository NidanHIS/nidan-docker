# Minimal placeholder for OpenELIS. Update with real values as needed.
# Database settings are passed via CATALINA_OPTS; adjust here only if required.

system.instance.name=Nidan OpenELIS
system.environment=dev

# Base URLs (adjust to your domain)
system.base.url=https://localhost/openelis

# Email placeholders
mail.smtp.host=localhost
mail.smtp.port=25
mail.from=no-reply@localhost


