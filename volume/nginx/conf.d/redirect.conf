# Generic HTTP → HTTPS redirect
server {
    listen 80;
    server_name _;
    return 301 https://$host$request_uri;
}

# OpenELIS specific redirect
server {
    listen 80;
    server_name openelis.localhost;
    return 301 https://openelis.localhost$request_uri;
}


